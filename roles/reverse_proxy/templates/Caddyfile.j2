{# TODO: Split this out maybe #}
(cloudflare) {
  tls {
    dns cloudflare {{ cloudflare_token }}
  }
}
# immich
photos.{{ domain_name }} {
  import cloudflare
  reverse_proxy localhost:{{ immich_port }}
}
# freshrss
feeds.{{ domain_name }} {
  import cloudflare
  reverse_proxy localhost:{{ freshrss_port }}
}
# memos
blog.{{ domain_name }} {
  import cloudflare
  reverse_proxy localhost:{{ memos_port }}
}
# forgejo
git.{{ domain_name }} {
  import cloudflare
  reverse_proxy localhost:{{ forgejo_port }}
}
# syncthing
sync.{{ domain_name }} {
  import cloudflare
  reverse_proxy localhost:{{ syncthing_port }}
}
# cockpit
server.{{ domain_name }} {
  import cloudflare
  reverse_proxy localhost:{{ cockpit_port }} {
    transport http {
      tls_insecure_skip_verify
    }
  }
}
# karakeep
links.{{ domain_name }} {
  import cloudflare
  reverse_proxy localhost:{{ karakeep_port }}
}
# karakeep-homedash
home.{{ domain_name }} {
  import cloudflare
  reverse_proxy localhost:{{ karakeep_homedash_port }}
}
# emby
media.{{ domain_name }} {
  import cloudflare
  reverse_proxy localhost:{{ emby_port }}
}
# calibre
https://ebook-gui.{{ domain_name }} {
  import cloudflare
  reverse_proxy localhost:{{ calibre_port }}
}
# calibre-web
https://ebooks.{{ domain_name }} {
  import cloudflare
  reverse_proxy localhost:{{ calibre_web_port }}
}
# obsidian
https://notes.{{ domain_name }} {
  import cloudflare
  reverse_proxy localhost:{{ obsidian_port }}
}
