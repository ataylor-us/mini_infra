---
- name: Reload systemd for emby
  ansible.builtin.systemd:
    daemon_reload: true
  become: true
  changed_when: true
- name: Restart emby
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: restarted
  become: true
  loop: "{{ services }}"
