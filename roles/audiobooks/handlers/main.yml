---
- name: Reload systemd for audiobooks
  ansible.builtin.systemd:
    daemon_reload: true
  become: true
  changed_when: true
- name: Restart audiobooks
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: restarted
  become: true
  loop: "{{ services }}"
